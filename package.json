{
  "name": "prisma-enum",
  "version": "1.2.0",
  "description": "CLI tool for exporting enums from @prisma/client to reduce bundle size.",
  "main": "index.js",
  "types": "index.d.ts",
  "author": "Richard Xiong",
  "license": "MIT",
  "dependencies": {
    "@tailwindcss/forms": "^0.5.3",
    "express": "^4.18.1"
  },
  "scripts": {
    "prepare": "yarn build",
    "build": "tailwindcss -i ./tailwind.css -o ./public/__componentStyles.css",
    "tailwind": "tailwindcss -i ./tailwind.css -o ./public/__componentStyles.css -w"
  },
  "devDependencies": {
    "@types/express": "^4.17.14",
    "tailwindcss": "^3.1.8"
  }
}
